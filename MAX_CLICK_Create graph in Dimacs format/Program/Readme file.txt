Here's a complete, ready-to-use C program that:

Generates all permutations of size n ≤ 9

Builds a graph where there's an edge between two permutations if their Levenshtein distance ≥ d

Outputs the graph in DIMACS format to a file